# Screenshots

Drop the README images here (the README references these exact names):

- `hero.png` — the main dashboard: two groups, a focused tile, a blocked agent.
- `glance.png` — glance mode (`z`): compact keep-an-eye cards.
- `picker.png` — the project picker (`^n`).

Capture them by running `omni` in a real terminal at a roomy size.
