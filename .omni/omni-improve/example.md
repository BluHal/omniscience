# example agent

The filename is this agent's **role** (`example`); this file's contents are its
**brief** — the instructions it reads and starts working on.

Replace this with a real task. Add one `.md` per agent you want in the room
(e.g. `frontend.md`, `backend.md`). Then launch:

    omni up omni-improve
